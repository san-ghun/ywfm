ywfm Improvement Tasks:

High Priority:
  ✔ Fix type hints in ReminderConfig dataclass @file(src/main.py:37-47) @done
    - Change `subject: str = None` to `subject: Optional[str] = None`
    - Apply to all Optional fields
  ✔ Remove dead code (empty else branch) @file(src/main.py:177-178) @done
  ✔ Fix typo "installiing" in install.py @file(install.py:134) @done
  ✔ Create tests directory structure @done
    ✔ Create tests/__init__.py @done
    ✔ Create tests/test_config.py with ReminderConfig tests @done
    ✔ Create tests/test_manager.py with ReminderManager tests @done
  ✔ Add .pre-commit-config.yaml with ruff hooks @done
  ✔ Verify all changes pass linting and type checking @done

Medium Priority:
  ✔ Implement --list command to show active reminders @done
    ✔ Read JSON files from ~/.local/state/ywfm/ @done
    ✔ Check if PIDs are still running @done
    ✔ Display in table format @done
  ✔ Implement --cancel command to stop reminders @done
    ✔ Accept PID or name as argument @done
    ✔ Send SIGTERM to process @done
    ✔ Clean up state files @done
  ✔ Add --name option for named reminders @done
    ✔ Store name in JSON output @done
    ✔ Create name -> PID mapping @done
  ✔ Add --dry-run mode @done
    ✔ Show configuration without executing @done
    ✔ Calculate trigger time @done
  ☐ Add configuration file support (~/.config/ywfm/config.toml) @future
  ☐ Add --repeat option for recurring reminders @future

Code Simplification:
  ✔ Refactor time_limit flag to computed property (is_time_limited) @done
  ✔ Move sys.stdout.flush() before fd closing in daemonize() @done
  ✔ Remove description field, compute dynamically @done
  ✔ Extract common execution logic to _trigger_reminder() method @done

Platform Support:
  ✔ Add package manager detection for Linux @done
    ✔ Support apt (Debian/Ubuntu) @done
    ✔ Support dnf (Fedora/RHEL) @done
    ✔ Support yum (CentOS/older RHEL) @done
    ✔ Support pacman (Arch) @done
    ✔ Support zypper (openSUSE) @done
  ✔ Add fallback manual installation instructions @done
  ☐ Test on multiple distributions @future

Documentation:
  ✔ Create improvement plan documentation @done
  ✔ Add CHANGELOG.md @done
  ☐ Update README.md with new features @future
  ✔ Add CLAUDE.md for Claude Code guidance @done

Archive:
  Completed: 2026-01-04
  Commits: 9
  Tests: 30 passing
